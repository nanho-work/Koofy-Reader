rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Downloads use short-lived URLs issued for published assets by the server.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
