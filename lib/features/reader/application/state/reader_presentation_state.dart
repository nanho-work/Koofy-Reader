class ReaderPresentationState {
  const ReaderPresentationState({
    required this.hasQueryMatches,
    required this.shouldShowFooterAd,
  });

  final bool hasQueryMatches;
  final bool shouldShowFooterAd;
}
